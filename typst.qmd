---
format: typst
author: Vincent Arel-Bundock
title: Testing
---

# Level 1

```{=typst}
#lorem(30)
```

## Level 2

```{r}
library(modelsummary)
mod <- list(
    lm(mpg ~ hp, mtcars),
    lm(mpg ~ hp + I(qsec^2), mtcars)
)
modelsummary(mod, coef_rename = TRUE, output = "typst")
```